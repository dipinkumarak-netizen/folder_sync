import 'package:flutter/material.dart';

/// =======================================================
/// FTP Backup
/// App Color Palette
/// AMOLED Deep Black Theme
/// =======================================================

class AppColors {
  AppColors._();

  // Background Colors
  static const Color background = Color(0xFF000000);
  static const Color surface = Color(0xFF121212);
  static const Color card = Color(0xFF1A1A1A);

  // Primary Colors
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryDark = Color(0xFF1565C0);

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFBDBDBD);
  static const Color textHint = Color(0xFF757575);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF03A9F4);

  // Feature Colors
  static const Color ftp = Color(0xFF2196F3);
  static const Color backup = Color(0xFF00BCD4);
  static const Color restore = Color(0xFF9C27B0);
  static const Color sync = Color(0xFF4CAF50);
  static const Color schedule = Color(0xFFFFC107);
  static const Color history = Color(0xFFFF7043);
  static const Color settings = Color(0xFF9E9E9E);

  // Borders & Divider
  static const Color border = Color(0xFF2A2A2A);
  static const Color divider = Color(0xFF303030);

  // Icon Colors
  static const Color icon = Colors.white;
  static const Color iconInactive = Color(0xFF757575);

  // Progress
  static const Color progressBackground = Color(0xFF2B2B2B);

  // Shadow
  static const Color shadow = Colors.black54;
}